<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div id="login-wrapper">
      <div class="panel panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title">
            회원 가입하기
          </h3>
        </div>
        <div class="panel-body">
          <%= devise_error_messages! %>
          <p>당신의 이메일 주소로 로그인해주세요.</p>
            <%=form_tag('/users', method: :post, class: "form-horizontal")%>
            <div class="form-group">
              <div class="col-md-12">
                <input type="email" class="form-control" name="user[email]" id="email" placeholder="Email" autofocus="true">
                <i class="fa fa-user"></i>
              </div>
            </div>
            <div class="form-group">
              <div class="col-md-12">
                <input type="password" class="form-control" name="user[password]" id="password" placeholder="Password">
                <i class="fa fa-lock"></i>
              </div>
            </div>
            <div class="form-group">
              <div class="col-md-12">
                <input type="password" class="form-control" name="user[password_confirmation]" id="password" placeholder="Password Confirmation">
                <i class="fa fa-lock"></i>
                <% if @validatable %>
                <em>(<%= @minimum_password_length %> characters minimum)</em>
                <% end %><br />
              </div>
            </div>
            <div class="form-group">
              <div class="col-md-12">
                <button type="submit" class="btn btn-primary btn-block">가입하기</button>
                <hr />
                <a href="/users/sign_in" class="btn btn-default btn-block">이미 계정이 있으신가요? [ 클릭 ]</a>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
